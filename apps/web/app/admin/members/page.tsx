import { MembersPanel } from '@/features/admin/components/members-panel';

/** Members management — invite, roles, removal, pending invitations. */
export default function AdminMembersPage(): React.JSX.Element {
  return <MembersPanel />;
}
