import { OverviewPanel } from '@/features/admin/components/overview-panel';

/** Admin console landing — org snapshot and quick links. */
export default function AdminOverviewPage(): React.JSX.Element {
  return <OverviewPanel />;
}
