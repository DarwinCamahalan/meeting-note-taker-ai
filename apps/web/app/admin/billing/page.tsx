import { BillingPanel } from '@/features/admin/components/billing-panel';

/** Seats & billing — seat accounting and the Stripe Customer Portal link. */
export default function AdminBillingPage(): React.JSX.Element {
  return <BillingPanel />;
}
