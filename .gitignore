# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
out/
# electron-builder installer output (electron-builder.yml directories.output: release).
# NOTE: bare `build/` and `release/` are NOT ignored — they collide with source dirs
# (apps/desktop/build = electron-builder buildResources; apps/web/lib/release = code).
apps/desktop/release/
.next/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Turborepo
.turbo/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
