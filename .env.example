# Cue — Phase 0 spike secrets. Copy to .env and fill in. NEVER commit .env.
# Anthropic API key (Claude — claude-haiku-4-5 streaming cues)
ANTHROPIC_API_KEY=
# Deepgram API key (nova-2 live streaming STT)
DEEPGRAM_API_KEY=
